# 2026-04-22 工作日记

## 晚间（/endday）

### ✅ 完成的工作

- 复查 A 组带宽实验后台进程与结果目录，确认当前无活跃 `run_all_experiments.py` 任务
- 核对 `最后实验/横坐标任务 带宽不同/` 下 `bup_2e7`、`bup_3e7`、`bup_4e7` 的 `tasks=5..50` 全部完整落盘
- 更新 `最后实验/横坐标任务 带宽不同/横坐标任务-带宽对比实验.csv`，补入 `2e7/3e7/4e7` 三组 A 组列，并按可行率优先、成本次之重算 `最佳可行方案`
- 整理最终实验目录：新增 `最后实验/横坐标任务 时延不同/` 与 `最后实验/横坐标任务 消融/` 的归档结构，并把任务数消融 CSV 收口到子目录下

### 📝 修改文件

- `.claude/rules/progress.md`
- `日记/2026-04-22.md`
- `最后实验/横坐标任务 带宽不同/横坐标任务-带宽对比实验.csv`
- `最后实验/横坐标任务 消融/横坐标任务数 消融实验.csv`
- `最后实验/横坐标任务数 消融实验.csv`（删除）
- `最后实验/横坐标任务 带宽不同/bup_2e7/`
- `最后实验/横坐标任务 带宽不同/bup_3e7/`
- `最后实验/横坐标任务 带宽不同/bup_4e7/`
- `最后实验/横坐标任务 时延不同/`

### 📊 当前结果结论

- A 组带宽 sweep 现在已覆盖 `bup=1e7/2e7/3e7/4e7/5e7`
- 当前带宽对比表中：`tasks=10/15/40` 最优为 `A@3e7`，`tasks=20` 为 `A@2e7`，`tasks=30/50` 为 `A@4e7`，其余代表性点位仍多由 `A@5e7` 占优
- 当前没有待补跑的 `bup_2e7/3e7/4e7` 带宽实验后台任务

### ⚠️ 已知卡点

- `最后实验/横坐标任务 带宽不同/README.md` 还没有反映 `2e7/3e7/4e7` 三组新目录
- `文档/30_测试与执行/最终实验现状总览_2026-04-21.md` 还没同步这批新整理结果
- 本次没有运行 `uv run pytest tests -v`

### 📌 下次开始建议

1. 补更新带宽目录 README 与最终实验总览文档
2. 如要统一论文图表，优先整合 bandwidth / tau / task-ablation 三张表
3. 若继续动脚本或汇总逻辑，先跑 `uv run pytest tests -v`
