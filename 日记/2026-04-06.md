# 2026-04-06 工作日记

## ✅ 完成的工作

- 提交积压的未跟踪文件（3 个文档）：
  - `MEMORY.md`：项目进度记录文件（新建）
  - `文档/40_审查与诊断/BCD迭代与SCA数值诊断_2026-04-05.md`：昨日诊断记录
  - `文档/60_规划草案/HS替代Layer1求解器_可行性分析_2026-04-06.md`：HS-L1 可行性分析

- **Codex + Gemini 联合参数讨论**（多 Agent teammate 模式）：
  - Gemini：tau=0.5s 对大任务过紧；alpha=35 时通信项贡献远小于飞行项（175 vs 2000）
  - Codex：`adapt_f_edge` 置 0 是根本 Bug；converged 几乎恒 True
  - 讨论结果写入 `文档/40_审查与诊断/diagnosis_bcd_param_discussion_20260406.md`

- **BCD 三 Bug 修复**（`edge_uav/model/bcd_loop.py`）：
  1. `adapt_f_edge_for_snapshot`：未选中边 `0.0` → `uav.f_max / N_tasks` 保底值
  2. `cost_history.append(cost_new)` 替代 `best_cost`
  3. `converged_flag` 替代 `len(cost_history) < max_bcd_iter + 1`
  - 新增 `TestAdaptFEdgeForSnapshot`（3 tests）+ `TestBCDConvergenceBehaviour`（3 tests）
  - 全量回归：**157 passed, 2 skipped**

- **BCD 收敛行为诊断**（D1 seed=42 实验）：
  - 禁用收敛 break 强制 5 轮，确认 k=1 后 L1 完全锁定（不动点）
  - BCD 2 轮收敛是正确行为，鞍点是 BCD 结构性局限（MIP 整数 + 坐标下降）
  - HS+LLM 多样性的价值 = 引导 BCD 落入不同鞍点

## 📝 修改文件

- `edge_uav/model/bcd_loop.py` (+19, -8)：三 Bug 修复
- `tests/test_bcd_loop.py` (+154, -1)：6 个新单元测试
- `config/setting.cfg`：alpha 调为 350（实验用）
- `heuristics/hsPopulation.py`：timeout 改回 300（临时改 3600 后撤回）
- `文档/40_审查与诊断/diagnosis_bcd_param_discussion_20260406.md`（新建）

## ⏳ 进行中

- A+D1 三 seed（42/43/44）对比实验后台运行中（验证 LLM 是否能跨越鞍点）

## ⚠️ 当前已知问题

- BCD 2 轮即收敛，eps_bcd=1e-3 正确触发，但 BCD 本质上是坐标下降易陷鞍点
- A 组 LLM 并行调用偶发超时（300s 内 1/10 future 未完成），可能是网络抖动

## 📌 下次开始建议

1. **等 A+D1 实验结果**：汇总 3 seed 的 A vs D1 成本对比，验证 LLM 是否能跨越鞍点
2. **若 A 没有改善**：考虑方案 B（收敛判据改为 L1 决策稳定性检测）
3. **参数调整验证**：alpha=350 已在 setting.cfg，跑完整对比后决定是否保留
