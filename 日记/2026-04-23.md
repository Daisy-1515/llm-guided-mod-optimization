# 2026-04-23 工作日记

## 晚间（根据 Git 记录整理）

### ✅ 完成的工作

- 新增 `AGENTS.md`，把当前仓库协作约定、目录说明与工作方式单独收口，便于后续多代理/多轮协作复用
- 新增 `文档/20_架构与实现/LLM_HS_流程与反馈机制详解_2026-04-22.md`，系统整理子问题一中 LLM-HS 的代理目标函数生成、回退分支、Harmony Search 演化与统一外部评估逻辑
- 更新 `文档/INDEX.md`，补上上述机制文档入口
- 统一回填三张最终实验汇总表：
  - `最后实验/横坐标任务 带宽不同/横坐标任务-带宽对比实验.csv`
  - `最后实验/横坐标任务 时延不同/横坐标任务-时延对比实验.csv`
  - `最后实验/横坐标任务 消融/横坐标任务数 消融实验.csv`
- 为三类横坐标实验补齐 MATLAB 作图脚本：
  - 带宽对比：`plot_task_bandwidth_cost.m`
  - 时延对比：`plot_task_delay_comparison_cost.m`
  - 无人机数量消融：`plot_uav_ablation_cost.m`
- 调整任务数消融作图脚本与表格标签，使曲线命名统一到 `本地执行 / 标准目标函数 / LLM / LLM+HS` 口径
- 为无人机数量消融补入 `LLM(B)` 与 `本地执行` 基线结果，新增 `u=3~9` 对应的 `B/`、`LOCAL/`、`comparison_summary.json` 与 `manifest.json`
- 更新 `最后实验/横坐标无人机 消融实验/README.md` 与 `横坐标无人机数量 消融实验.csv`，把无人机消融统一扩展到四条对比曲线
- 晚间补提交 `最后实验/横坐标任务 消融/plot_task_ablation_cost.asv`，把任务数消融绘图脚本的 MATLAB 自动保存副本纳入版本库

### 📝 修改文件

- `AGENTS.md`
- `文档/20_架构与实现/LLM_HS_流程与反馈机制详解_2026-04-22.md`
- `文档/INDEX.md`
- `最后实验/横坐标任务 带宽不同/横坐标任务-带宽对比实验.csv`
- `最后实验/横坐标任务 带宽不同/plot_task_bandwidth_cost.m`
- `最后实验/横坐标任务 时延不同/横坐标任务-时延对比实验.csv`
- `最后实验/横坐标任务 时延不同/plot_task_delay_comparison_cost.m`
- `最后实验/横坐标任务 消融/横坐标任务数 消融实验.csv`
- `最后实验/横坐标任务 消融/plot_task_ablation_cost.m`
- `最后实验/横坐标任务 消融/plot_task_ablation_cost.asv`
- `最后实验/横坐标无人机 消融实验/README.md`
- `最后实验/横坐标无人机 消融实验/plot_uav_ablation_cost.m`
- `最后实验/横坐标无人机 消融实验/横坐标无人机数量 消融实验.csv`
- `最后实验/横坐标无人机 消融实验/u3~u9/` 下新增的 `B` / `LOCAL` 结果目录与汇总文件

### 📊 当前结果结论

- 任务数消融表当前已经统一为四条主曲线：`本地执行`、`标准目标函数`、`LLM`、`LLM+HS`
- 从任务数消融成本表看，`LLM+HS` 在 `numTasks=5~50` 的全部点位上都给出了当前最低成本
- 从无人机数量消融表看，`u=3/7` 的最佳可行方案为 `LLM+HS`，`u=4/5/6/8/9` 的最佳可行方案为 `标准目标函数`
- 当前无人机数量消融目录已形成四组统一口径：
  - `本地执行`
  - `标准目标函数(D1)`
  - `LLM(B)`
  - `LLM+HS(A)`
- 当前带宽对比表中，最佳可行方案分布为：
  - `tasks=10/15/40` 为 `A@3e7`
  - `tasks=20` 为 `A@2e7`
  - `tasks=30/50` 为 `A@4e7`
  - 其余点位多为 `A@5e7`
- 当前时延对比表中，`tasks=10/15/20/35` 最优为 `B`，`tasks=25/30/40/45/50` 最优为 `D1`，仅 `tasks=5` 为 `A@2.0`

### ⚠️ 已知卡点

- 今日提交主要集中在结果整理、文档归纳与 MATLAB 作图脚本补齐，没有新增 Python 侧自动化校验
- `最后实验/横坐标任务 消融/plot_task_ablation_cost.asv` 属于 MATLAB 自动保存副本，后续若追求仓库整洁，可以考虑是否继续纳入版本控制
- 当前多张 CSV 已形成论文图表基础，但还没有统一导出成最终成图与论文插图说明
- 本次未运行 `uv run pytest tests -v`

### 📌 下次开始建议

1. 基于三类横坐标实验的最新 CSV，统一导出正式论文图
2. 决定是否保留 `.asv` 自动保存文件进入仓库
3. 将今天整理出的带宽 / 时延 / 任务数 / 无人机数四类结论同步到最终实验总览或论文材料文档
4. 若后续继续改脚本或汇总逻辑，先跑一次 `uv run pytest tests -v`
