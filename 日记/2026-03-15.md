# 2026年3月15日 周日

## 项目：边缘无人机计算卸载系统 — 场景生成器步骤 1-2 + Gurobi 验证

---

今天推进了场景生成器的前两步实现，并验证了 Gurobi 许可证可用性。

### 步骤 1：冻结 EdgeUavScenario dataclass 接口

在 `dataCommon.py` 末尾新增 `EdgeUavScenario` dataclass（~155 行），三方协作完成。

**实现内容**：
- 5 个字段：`tasks`、`uavs`、`time_slots`、`seed`、`meta`
- 序列化：`_task_to_dict`、`_uav_to_dict`、`to_dict`
- 反序列化：`_task_from_dict`、`_uav_from_dict`、`from_dict`（classmethod）
- 文件 I/O：`save_json`、`load_json`（classmethod）

**Codex Review 发现 4 个问题，全部修复**：

| # | 问题 | 修复 |
|---|---|---|
| 1 | meta 中 `depot_pos` tuple 落盘后丢失类型 | `to_dict` 显式转 list，`from_dict` 还原 tuple |
| 2 | outer key 与 inner index 可能不一致 | `from_dict` 加 assert 校验 |
| 3 | `_task_from_dict` 忽略 active 值 | 改为按 value 过滤 |
| 4 | 类型注解过弱 | 补全为 `dict[int, ComputeTask]` 等 |

**测试**：7/7 全部通过（to_dict、from_dict 闭环、active 过滤、key 篡改检测、save/load、类型注解）。

之后为 EdgeUavScenario 添加了完整的语法注释和功能注释。

### 步骤 2：扩展 Config（3 节 15 参数）

**`setting.cfg` 新增**：
- `[edgeUavTask]`（12 参数）：D_l/D_r/F/tau 的 min/max、f_local_default、active_mode、active_window_min/max
- `[edgeUavDepot]`（2 参数）：depot_x、depot_y
- `[edgeUavSeed]`（1 参数）：scenario_seed

**`config.py` 新增**：15 个默认值 + 15 行解析逻辑

**Cast 类型**：float（数值范围/坐标）、int（窗口长度/种子）、str（active_mode 无 cast）

**Codex Review**：无阻塞问题。残余风险（跨字段校验）正确归属步骤 5。

**测试**：15/15 参数值+类型全部验证通过。

### Gurobi 许可证验证

| 项目 | 结果 |
|---|---|
| 版本 | gurobipy 13.0.1 |
| 许可证 | Restricted license（非生产），2027-11-29 到期 |
| LP 求解 | 通过 |
| MIP (binary) | 通过 |
| 2000 变量 | 通过 |
| 5000 变量 | **受限**（"Model too large for size-limited license"） |

当前项目规模（numTasks=10, numUAVs=3, T=20）→ 600 个 binary 变量，远在限制内。扩大到 30 设备（1800 变量）仍安全。50+ 设备需申请学术许可证。

### 今日变更

| 文件 | 变更 |
|---|---|
| `dataCommon.py` | +233 行（EdgeUavScenario dataclass + 注释） |
| `config/setting.cfg` | +21 行（3 个新配置节） |
| `config/config.py` | +44 行（15 默认值 + 15 解析） |

### 当前进度

```
✅ 数学建模文档
✅ Prompt 模板（edgeUavPrompt.py + edgeUavModPrompt.py）
✅ edgeUavOffloadingModel（Level 1 BLP）
✅ ① 数据结构（ComputeTask + UAV）
✅ ①.5 Config 基础适配（7 节 27 参数）+ ②-步骤2 扩展（3 节 15 参数）= 共 10 节 42 参数
✅ ② 场景生成器设计方案
✅ ②-步骤1 EdgeUavScenario dataclass（含序列化）
✅ ②-步骤2 Config 扩展（3 节 15 参数）
✅ Gurobi 验证（v13.0.1，受限许可证，600 变量安全）
⬜ ②-步骤3~6 场景生成器核心实现 ← 下一步
⬜ ②-步骤7~8 Smoke Test + Review
⬜ ③ 预计算模块
⬜ ④ hsIndividual 适配 + stub Level2
⬜ ④.5 端到端联调
⬜ ⑤ Level 2 真实实现
⬜ ⑥ 仿真器
```

### 下次开始的第一件事

**场景生成器步骤 3-6**：搭骨架 → 核心生成逻辑 → 校验 → 序列化集成（四步关联紧密，适合一起做）。
