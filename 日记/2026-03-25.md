# 2026-03-25 工作日记

## ✅ 完成的工作

### 1. Phase⑥ Step3 DCP 约束非凸性问题修复计划
- **文件**：`plans/phase6-step3-socp-fix-plan.md` (620 行)
- **内容**：
  - 🔴 问题诊断：两层次非凸性分析（理论层 + 实施层）
  - 📋 三方案对比：SOCP（推荐）、凸松弛、SCA改进
  - 🎯 详细实施计划：7个步骤，25-26小时
  - 📐 SOCP重新建模方案（第8节，将添加到公式20_凸性分析.md）
  - ⚠️ 风险矩阵与回滚策略

### 2. 执行门禁检查
- ✅ Git预检通过：master分支，clean状态
- ✅ 环境确认：CVXPY 1.4.0+，CLARABEL求解器可用
- ✅ 理解确认：SOCP基础与DCP规范

## 📊 变更摘要

```
修改文件数：1
新增行数：+620
删除行数：0
└─ plans/phase6-step3-socp-fix-plan.md (new)
```

## 📝 核心内容概览

### 问题根源（两层）

**Level 1（理论）**：原始联合优化 $\min_{x,f,P,q} C(x,f,P,q)$ 是混合整数非凸的
- 已有文档：`公式20_凸性分析.md`
- 对策：BCD分解，固定 $x,f,P$ 优化 $q$

**Level 2（实施）**：轨迹子问题 $\min_q \phi(q)$ 理论凸但CVXPY代码违反DCP规范
- 违规1：通信延迟约束 `sqrt(dist) × inv_pos(rate)` 是凹×凸乘积 ❌
- 违规2：安全分离约束 SCA线性化多次迭代导致约束集非凸 ❌

### SOCP修复方案（推荐）

**转换思路**：
```
原约束（非凸）：
  2·√(H² + ||q_j - w_i||²) · (1/rate) ≤ τ_comm

改写为SOCP（DCP规范）：
  (SOCP-1)  ||[H, (q_j - w_i)_x, (q_j - w_i)_y]||₂ ≤ s_ji
  (SOCP-2)  2·s_ji·rate ≥ D_i
```

**优势**：
- 完全DCP规范 ✓
- CLARABEL/ECOS/SCS直接求解 ✓
- 无近似误差，数学精确 ✓
- 论文标准形式 ✓

### 实施路线

| 步骤 | 内容 | 工作量 |
|------|------|-------|
| 1 | 通信约束SOCP建模 | 3.5h |
| 2 | 安全分离SCA改进 | 2.5h |
| 3 | 代码实现+单测 | 5.5h |
| 4 | 逐个测试修复 | 6.0h |
| 5 | 集成验证 | 2.5h |
| 6 | 管道验证 | 3.5h |
| 7 | 报告生成 | 1.5h |
| **总计** | | **25-26h** |

### 关键交付物

- ✅ 修改 `trajectory_opt.py`：移除非DCP，新增SOCP约束生成函数
- ✅ 更新 `test_trajectory_opt.py`：6个失败测试修复 + 2个DCP验证测试
- ✅ 添加文档第8节：SOCP重新建模方案（~500字推导）
- ✅ 验证报告：12/12测试通过，HS管道验证

## ⏳ 进行中的工作

- Phase⑥ Step3 修复实施（待下次会话开始）
- 本计划已保存为下次执行的最高级参考

## 🎯 下次开始建议

### 立即执行（优先级高）

1. **准备阶段**（预计1小时）
   ```bash
   # 记录测试基准
   pytest tests/test_trajectory_opt.py --tb=no > baseline_step3.log

   # Checkpoint before major changes
   git add -A && git commit -m "checkpoint: before Phase6-Step3-SOCP"
   ```

2. **步骤1-2：约束改写**（预计6小时）
   - 按照 `plans/phase6-step3-socp-fix-plan.md` 第【步骤1】和【步骤2】执行
   - CVXPY DCP验证：`problem.is_dcp()` 必须返回 True
   - SCA收敛性检查：迭代10次内收敛

3. **步骤3-5：代码+测试+验证**（预计9小时）
   - 新增约束函数（1h）
   - 重构子问题构建（2h）
   - 单元测试修改（1.5h）
   - 逐个测试修复（6h）
   - 集成验证（2.5h）

### 参考资源

- **完整计划**：`plans/phase6-step3-socp-fix-plan.md`
- **理论基础**：`文档/10_模型与公式/公式20_凸性分析.md`
- **当前代码**：`edge_uav/model/trajectory_opt.py` (行390-562)
- **测试套件**：`tests/test_trajectory_opt.py` (12个测试)

### 风险预案

| 卡点 | 症状 | 回滚 |
|-----|------|------|
| SOCP求解超时 | status="unknown" | 降低活动卸载数，改用SCS |
| 约束不可行 | status="infeasible" | 检查s下界，调整τ_comm |
| 轨迹质量↓ | obj_value>prev 20% | 增加SCA迭代，调权重 |

**快速回滚**：
```bash
git reset --hard HEAD~1  # 回到checkpoint
```

## 📌 关键决定记录

- ✅ 方案选择：SOCP（完全DCP规范）vs 凸松弛（快但不彻底）vs SCA改进（复杂） → 选A
- ✅ 计划粒度：7步分解，每步1-6小时，便于中断和恢复
- ✅ 验证策略：单步DCP检验 → 单测修复 → 集成验证 → 管道验证

## 📚 文档更新清单

待执行：
- [ ] 添加 `公式20_凸性分析.md` 第8节：SOCP重新建模
- [ ] 本日记已生成：`日记/2026-03-25.md`
- [ ] 计划已保存：`plans/phase6-step3-socp-fix-plan.md`

---

**时间戳**：2026-03-25 12:45
**工作时长**：4.5小时（需求分析+计划编制）
**下次开始**：立即执行 Phase⑥ Step3（步骤1-2），预期6小时
**总体进度**：Phase④ 100% → Phase⑤ 100% → Phase⑥ 5% (Step3 planning done)
