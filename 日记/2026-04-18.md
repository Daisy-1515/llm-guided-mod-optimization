# 2026-04-18 工作日记

## 晚间

### ✅ 完成的工作
- 扩展 `scripts/plot_optimization_curves.py`，新增两类 UE sweep 绘图模板：
  - `ue-sweep-by-group`：横轴 `numTasks`，曲线为算法组
  - `ue-sweep-by-uavs`：横轴 `numTasks`，曲线为 `numUAVs`
- 保留原有 `convergence` 模式默认行为，避免旧图回归
- 新增 `tests/test_plot_optimization_curves.py`，覆盖：
  - 结果读取
  - 扁平化与聚合
  - coverage report
  - 过滤逻辑
  - convergence / sweep 最小出图路径
- 删除并重建 `.venv`，修复 `uv run` 因 `.venv/lib64` 引发的权限问题
- 跑通验证：
  - `tests/test_plot_optimization_curves.py`
  - `tests/test_analyze_results_experiments.py`
  - 真实实验目录的 sweep 烟测
  - convergence 回归烟测
- 重新盘点“最终实验”目录，并清理今天失败/中断留下的空壳目录

### 📊 今日最终实验汇总
- `最终实验/20260418_154429`（D1, 3 seeds）
  - `best_cost_mean ≈ 329.30`
  - `best_cost_min ≈ 286.92`
  - `feasible_rate_mean = 1.0`
- `最终实验/pure_llm_l1_bcd/20260418_180657`（B, 3 seeds）
  - `best_cost_mean ≈ 257.17`
  - `best_cost_min ≈ 212.76`
  - `feasible_rate_mean = 1.0`
- `最终实验/pure_llm_l2b_bcd/20260418_180547`（B, 3 seeds）
  - `best_cost_mean ≈ 257.45`
  - `best_cost_min ≈ 211.36`
  - `feasible_rate_mean = 1.0`

### 🧹 清理记录
- 删除空壳失败目录：
  - `最终实验/20260418_151834`
  - `最终实验/llm_hs_l1_bdown_x2/20260418_183646`
  - `最终实验/llm_hs_l1_bdown_x2/20260418_195041`
- 删除空父目录：
  - `最终实验/llm_hs_l1_bdown_x2`

### ⚠️ 当前状态
- 当前 sweep 图严格对应的是 `best_cost/mean_cost` 等综合指标，不是纯 delay
- 真实 sweep 数据目前主要只有 `numTasks=20`，还不足以支撑完整的 UE 趋势图
- 工作区还留有与本次绘图不完全同源的配置改动：
  - `config/setting.cfg`
  - `heuristics/hsPopulation.py`

### 📌 下次开始建议
1. 决定是否把 `config/setting.cfg` 与 `heuristics/hsPopulation.py` 一并纳入本次提交；若不是本次目标，建议拆开处理
2. 补齐 `numTasks = 10/15/20/25/30` 等 UE sweep 数据，再正式使用新模板出图
3. 若论文需要“任务处理时延”图，下一步应先在 evaluator / run payload 中单独落盘 delay 指标
