# 2026-04-05 工作日记

## ✅ 已完成的工作

### 文档整理（已提交）
- INDEX.md: 新增4条目 + 链接同步 + Phase归档说明
- CLAUDE.md: 日期修正 + 2026-04-04/05章节分立
- 5文件重命名: 日期格式统一 (git mv)
- archive/INDEX.md: 13归档文档完整记录
- 提交: `988fe5d`

### 聚合归一化设计
- 设计方案文档: `聚合归一化实现方案_2026-04-05.md`
- 目标函数更正说明: `目标函数同尺度归一化更正说明_2026-04-05.md`

## 📝 待提交的代码变更

### 核心模块（+291, -78）
| 文件 | 变更 | 说明 |
|------|------|------|
| `evaluator.py` | +53, -3 | 添加 N_act/N_fly 归一化因子 |
| `precompute.py` | +54, -0 | 添加推进参数和归一化计算 |
| `objectives.py` | +38, -4 | 目标函数调整 |
| `trajectory_opt.py` | +64, -8 | 轨迹优化调整 |
| `offloading.py` | +18, -1 | 卸载决策调整 |
| `bcd_loop.py` | +4, -0 | BCD 循环调整 |
| `resource_alloc.py` | +14, -2 | 资源分配调整 |

### 测试文件（+63, -17）
| 文件 | 变更 | 说明 |
|------|------|------|
| `test_bcd_config_plumbing.py` | +10 | 新增测试 |
| `test_bcd_loop.py` | +3 | 更新测试 |
| `test_phase6_step4_warmstart.py` | +23, -17 | 简化测试 |
| `test_resource_alloc.py` | +7 | 新增测试 |
| `test_trajectory_opt.py` | +20, -2 | 更新测试 |

### 文档更新（+61, -15）
| 文件 | 变更 | 说明 |
|------|------|------|
| `公式与两层解耦整合版_最新版_2026-03-24.md` | +60, -15 | 公式更新 |
| `聚合归一化实现方案_2026-04-05.md` | new | 设计方案 |
| `目标函数同尺度归一化更正说明_2026-04-05.md` | new | 更正说明 |

## ⏳ 进行中的工作

- 聚合归一化代码实现（需验证）

## 🔧 技术要点

### 聚合归一化公式实现
```python
# 归一化因子
N_act = sum(ζ_i^t for all tasks)  # active task-slot 总数
N_fly = |U| × (T-1)               # UAV 移动段总数

# 评分公式
score = (1/N_act) × delay_weight × delay_term
      + (1/N_act) × energy_weight × energy_comp_term
      + (1/N_fly) × energy_weight × energy_prop_term
      + deadline_weight × deadline_term
      + balance_weight × balance_term
```

### 关键变更
1. `PrecomputeResult` 新增 `N_act`, `N_fly` 属性
2. `evaluate_solution()` 使用归一化因子计算评分
3. `objectives.py` 各层级目标函数添加归一化

## 📌 下次开始建议

1. **验证聚合归一化实现**
   - 运行单元测试确认功能正确
   - 对比修正前后的轨迹优化结果

2. **提交代码变更**
   - 确认测试通过后提交聚合归一化相关变更
   - 建议提交消息模板：
     ```
     feat(normalization): 实现聚合归一化评分公式 (公式20)

     - evaluator.py: 添加 N_act/N_fly 归一化因子
     - precompute.py: 添加推进参数和归一化计算
     - objectives.py: 目标函数调整
     - trajectory_opt.py: 轨迹优化调整
     ```

3. **文档同步**
   - 确保代码与公式文档一致

---

## Session 3: SCA 求解器修复与参数调优

### ✅ 完成的工作

1. **F_max 参数修正** — 2.5e8→1.5e8（= tau × f_local），消除「任务本地不可完成 + 远距离卸载也超时」双重死锁
2. **Gurobi solver fallback** — `trajectory_opt.py` solver_fallback 首位加入 GUROBI，解决开源求解器（CLARABEL/ECOS/SCS）在 415 active_offloads 大规模 SOCP 下精度不足
3. **降带宽实验** — B_up/B_down 4e7→5e6，通信时延占比从 19%→55%，但 BCD 仍一轮收敛
4. **bcd_loop.py 清理** — 2 处 debug `print()` → `logger.info()`，保留增强 cost breakdown 信息
5. **测试调整** — `test_cost_monotonicity` 放宽容差至 5%（BCD L2b 引入 comm-delay 项导致微小超出）

### 📝 修改文件

| 文件 | 变更 | 说明 |
|------|------|------|
| `config/setting.cfg` | +3, -3 | F_max, B_up, B_down 参数调整 |
| `edge_uav/model/trajectory_opt.py` | +1, -1 | Gurobi 加入 solver fallback |
| `edge_uav/model/bcd_loop.py` | +8, -3 | print→logger.info + cost breakdown |
| `tests/test_phase6_integration.py` | +7, -2 | 放宽 monotonicity 容差 |

### 🔧 实验结论

- alpha=35 时 SCA 收敛，Gurobi 求解无问题
- 降带宽增加通信时延占比，但未触发 L1 决策翻转（BCD 一轮收敛）
- 多 UAV 轨迹分化问题仍未解决，通信牵引力不足

### 📌 下次开始建议

1. 继续调查 UAV 轨迹退化（alpha/lambda_w 调参 或 任务空间分布优化）
2. 运行完整规模 HS 实验
3. 分析诊断指标趋势