# 2026-04-04 工作日记

## ✅ 完成的工作

### BCD 联合目标修复（`feat(bcd): use complete L2 joint objective for cost rollback`）

- **问题**：`run_bcd_loop` 中 cost rollback 仅使用 `cost_traj`（L2b 轨迹目标），忽略了 `ra_result.objective_value`（L2a 本地计算目标）
- **修复**：`cost_new = ra_result.objective_value + cost_traj`，对应 L2-obj 全部 4 项（本地时延+计算能耗+卸载时延+飞行能耗）
- **附带**：两个测试 mock 补充 `objective_value` 字段

### D1 实验运行与轨迹诊断

- 运行 `run_all_experiments.py --groups D1 --seeds 42`，目标值 **243.42**
- 轨迹图生成：`discussion/experiment_results/20260404_210827/d1_trajectory_seed42.png`
- 速度验证：max **8.98 m/s**，violations **0**（上次 1038 m/s 的 bug 已完全修复）

## 📝 修改文件

| 文件 | 变更 | 说明 |
|------|------|------|
| `edge_uav/model/bcd_loop.py` | +34 -31 | cost rollback 改为完整 L2 联合目标 |
| `tests/test_bcd_config_plumbing.py` | +1 | mock 补 objective_value |
| `tests/test_bcd_loop.py` | +1 | mock 补 objective_value |

## ⚠️ 发现的问题

### 多 UAV 轨迹完全相同（未解决）

- D1 实验：3 架 UAV 轨迹完全相同，均为 `(0,0)→(500,500)` 直线，707m
- BCD cost history `[13430.39, 13430.39]`，第一次迭代就收敛，SCA 无分化
- **根因**：所有 UAV 共享相同 start/end depot，初始轨迹无差异，SCA 无法分化
- **影响**：多 UAV 协同优势完全丧失；D1 本身无 LLM 引导，此问题尤为突出

## 📌 下次开始建议

1. **调查 UAV 初始轨迹分化机制**：`make_initial_level2_snapshot` 是否为各 UAV 生成不同初始轨迹？
2. **修复多 UAV 轨迹退化**：引入初始轨迹扰动或 waypoint 分配策略
3. 提交 `analysis_20260404.md` 和 `analysis_20260404_trajectory.png`（当前在根目录，考虑移到 `文档/50_运行分析/`）
4. 运行完整规模 HS 实验（A vs D1 对比），验证 LLM 引导效果
