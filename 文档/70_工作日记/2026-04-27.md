# 2026-04-27 工作日记

## 中午至晚间（根据 Git 记录回填）

### ✅ 完成的工作

- 继续增强 `scripts/docx_to_md.py`：Markdown 目标改为 `markdown+tex_math_dollars+pipe_tables+raw_html`，加入 `--wrap=none` 和图片资源提取目录，并对 `pandoc` warning 做了更细的容错处理
- 新增 `scripts/generate_prompt_template_docx.py`，把当前 prompt operator 模板渲染成 paper-style 的 `.docx` 盒子文档，统一输出到 `文档/prompt_template_example.docx`
- 扩展 `tests/test_docx_to_md.py`，为 media 目录、`pandoc` 命令构造和 warning 场景补测试，避免 `docx_to_md` 只停留在“能跑一次”的状态
- 更新 `文档/20_架构与实现/当前提示词清单_2026-04-03.md`，同步当前提示词/模板整理结果
- 新增 `文档/prompt_template_example.docx` 和 `文档/prompt_template_example.md`，把模板生成结果和 Markdown 示例一起落盘，方便后续核查排版与内容

### 📝 修改文件

- `scripts/docx_to_md.py`
- `scripts/generate_prompt_template_docx.py`
- `tests/test_docx_to_md.py`
- `文档/20_架构与实现/当前提示词清单_2026-04-03.md`
- `文档/prompt_template_example.docx`
- `文档/prompt_template_example.md`

### 🧾 今日 Git 提交

| 时间 | Commit | 内容 |
|------|--------|------|
| 20:13 | `559021c` | `docs: end-of-day routine (2026-04-27)` |
| 13:52 | `b14182c` | `feat: improve docx to markdown conversion and add prompt template generator` |

### 📊 当日结论

- 白天的主工作实际上集中在 `b14182c`：6 个文件、`+1181 / -4`，核心主题是文档工具链和 prompt 模板产物，而不是 MoD/Edge-UAV 求解逻辑本身
- 晚上的 `559021c` 只是把当时自动生成的日记提交进仓库；原始自动日记只看到了这一次元提交，完全漏掉了白天的实质工作，所以才会出现“文件数 1、+0/-0”的假摘要
- 这一天的工作重心可以概括为两条线：一条是把 `.docx -> .md` 转换从“基础 CLI”升级到“能保留公式、表格和媒体资源”的可用工具，另一条是把 prompt operator 模板做成可直接查看的 `.docx` 成品

### ⚠️ 说明

- 这篇日记为事后根据 Git 记录回填；提交历史可以准确反映当天改动链，但不能直接证明当时是否跑过完整回归测试
- 从代码 diff 可以确认 `tests/test_docx_to_md.py` 在当天被继续扩展，因此这次不是单纯补文档，而是连同脚本行为和测试一起向前推进
- 原先自动生成的 2026-04-27 日记失真的直接原因，是它只读取了 endday 执行时那一刻的工作区状态，没有汇总当天已经提交过的 `b14182c`

### 📌 下次开始建议

1. 用包含图片、公式和表格的真实 `.docx` 样例跑一遍 `scripts/docx_to_md.py`，确认 `_media` 目录、Markdown 内容和 warning 处理都符合预期。
2. 若 `generate_prompt_template_docx.py` 要长期复用，补一段 README 或脚本使用说明，明确输出文件和排版目的。
3. 把文档工具链这条支线和主项目实验线分开记账，后续写日记时避免再次被 endday 的元提交盖住真正的开发内容。
