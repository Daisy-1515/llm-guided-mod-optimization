# 2026-04-03 工作日记

## ✅ 完成的工作

### 实验绘图脚本（plot_optimization_curves.py）

实现了一键生成所有实验目标函数收敛曲线的完整脚本：

- **数据层**：`scan_experiment_dirs` 扫描所有实验目录；`load_experiment_data` 加载 `best_so_far` + `history_by_gen`，按 seed × group 组织
- **静态图（matplotlib）**：2-subplot（左：收敛曲线，右：箱线图），固定 GROUP_COLORS，300dpi PNG 输出
- **交互图（plotly）**：同结构 HTML，支持 hover 显示数值
- **命令行**：`--exp-dir` 指定单目录，`--output-dir` 自定义输出路径

验证结果：
- 全量运行生成 25 PNG + 25 HTML，覆盖 16 个实验目录的所有有效种子
- 单组实验（仅 A 或仅 D1）正常渲染
- 空目录（20260402_235857 等仅 D1）正常处理，不崩溃
- `uv add plotly` 完成依赖管理（plotly 6.6.0 + narwhals 2.18.1）

### 依赖更新

- `pyproject.toml` 新增 `matplotlib>=3.10.8`、`plotly>=6.6.0`
- `uv.lock` 同步更新

## 📝 修改文件

| 文件 | 变更 | 说明 |
|------|------|------|
| `scripts/plot_optimization_curves.py` | +340（新建） | 主绘图脚本 |
| `pyproject.toml` | +2 | 新增 matplotlib/plotly 依赖 |
| `uv.lock` | +227 | 依赖锁定更新 |

## ⏳ 未处理项

- `scripts/plot_trajectory.py`（未跟踪，本会话未修改，待单独审视）
- `scripts/run_all_experiments.py`（未跟踪，本会话前已存在改动，待单独提交）
- 2 个 pre-existing 测试失败尚未修复（`test_bcd_metadata_recorded`、`test_condition_t_equals_0`）

## 📌 下次开始建议

1. 打开 `discussion/plots/20260402_191643/seed_44.png` 确认 5 组消融图效果
2. 运行 `uv run python scripts/plot_optimization_curves.py` 后查看 HTML 交互图
3. 修复 2 个 pre-existing 测试失败（`test_bcd_metadata_recorded` 键名、`test_condition_t_equals_0` LLM 依赖）
4. 提交 `scripts/plot_trajectory.py` 和 `scripts/run_all_experiments.py`（审视后单独提交）
