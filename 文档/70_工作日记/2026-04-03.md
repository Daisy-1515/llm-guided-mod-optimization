# 2026-04-03 工作日记

## ✅ 完成的工作

### 实验绘图脚本（plot_optimization_curves.py）

实现了一键生成所有实验目标函数收敛曲线的完整脚本：

- **数据层**：`scan_experiment_dirs` 扫描所有实验目录；`load_experiment_data` 加载 `best_so_far` + `history_by_gen`，按 seed × group 组织
- **静态图（matplotlib）**：2-subplot（左：收敛曲线，右：箱线图），固定 GROUP_COLORS，300dpi PNG 输出
- **交互图（plotly）**：同结构 HTML，支持 hover 显示数值
- **命令行**：`--exp-dir` 指定单目录，`--output-dir` 自定义输出路径

验证结果：
- 全量运行生成 25 PNG + 25 HTML，覆盖 16 个实验目录的所有有效种子
- 单组实验（仅 A 或仅 D1）正常渲染
- 空目录（20260402_235857 等仅 D1）正常处理，不崩溃
- `uv add plotly` 完成依赖管理（plotly 6.6.0 + narwhals 2.18.1）

### 依赖更新

- `pyproject.toml` 新增 `matplotlib>=3.10.8`、`plotly>=6.6.0`
- `uv.lock` 同步更新

## 📝 修改文件

| 文件 | 变更 | 说明 |
|------|------|------|
| `scripts/plot_optimization_curves.py` | +340（新建） | 主绘图脚本 |
| `pyproject.toml` | +2 | 新增 matplotlib/plotly 依赖 |
| `uv.lock` | +227 | 依赖锁定更新 |

## ✅ 完成的工作（第二会话）

### UAV 轨迹可视化（`scripts/plot_trajectory.py`）

新建 459 行 MATLAB 风格 2D 轨迹可视化脚本：

- **双图模式**：`--mode trajectory`（纯轨迹）/ `--mode assignment`（轨迹+分配关系）/ `--mode both`（默认）
- **MATLAB 风格**：Times New Roman 衬线体、内向刻度线、虚线网格、白底、等比例坐标
- **视觉元素**：3 色 UAV 轨迹（蓝/橙/绿）+ 方向箭头 + 时隙标号 + 起/终点标记 + 任务方块（■）+ 基站五角星（★）
- **分配关系**：从 `f_edge[j][i][t] > 0` 自动推断，UAV 颜色虚线连线，未分配任务灰色
- **CLI**：`--run-dir / --gen / --individual / --save / --no-show`，智能默认值（最新目录、最后代、最优个体）
- 验证通过：`discussion/20260402_231832 gen1` → `_trajectory.png` + `_assignment.png` 正常输出

### `run_all_experiments.py` BCD 路径增强（+62 行）

`solve_default_objective` 添加 `use_bcd_loop` 分支：
- 与 A/B/C 组一致的 Level-2 BCD 优化路径
- `dynamic_obj_func=None` 使用默认目标（无 LLM 引导）
- 基于最终 BCD 快照重新预计算后评分

## ✅ 完成的工作（第三会话）

### 过时文档归档清理

13 个已完成 Phase 的规划/诊断文档归档到 archive/ 子目录（与 Phase⑧ 惯例一致，可逆操作）：

**40_审查与诊断/archive/（10 个文件）**：
- `evaluator_drop兼容性修复计划_2026-03-31.md` — drop 已移除
- `Phase4_诊断指南_2026-03-27.md` — Phase④ 完成
- `Phase5_LLM调用问题诊断报告.md` — LLM 调用已修复
- `Phase6_Step3_SOCP_实施总结.md` — Step3 完成
- `Phase6_Step4_BCD_启用验证报告.md` — Step4 完成
- `代码清理审查报告_2026-03-23.md` — 代码库已大幅重构
- `项目文件清理分析.md` — 引用已删除文件
- `约束审计报告_2026-03-29.md` — L1-C2 已删除
- `第一层循环约束对齐结论_2026-03-29.md` — L1-C2 已删除
- `NMAX_VERIFICATION_REPORT.md` — 一次性验证快照

**20_架构与实现/archive/（3 个文件）**：
- `Phase6_BCD循环实施计划.md` — BCD 已实施
- `Phase6_Step2_resource_alloc详细实施计划.md` — 已落地
- `场景生成器设计方案.md` — 全部完成

### INDEX.md 同步更新

- 删除 12 条已归档文档条目
- 新增 3 条未收录文档（代码-公式差异记录、LLM vs D1 规模对比、实验汇总总览）
- 元数据更新：项目阶段、日期、最近会话
- "按开发阶段查找" 精简为活跃 Phase⑦~⑧ + 归档指引

## ⏳ 未处理项

- 2 个 pre-existing 测试失败尚未修复（`test_bcd_metadata_recorded` 键名、`test_condition_t_equals_0` LLM 依赖）
- 测试数据轨迹聚集在 depot 附近 — 低迭代运行特征，完整 HS 实验预计产生明显飞行轨迹

## 📌 下次开始建议

1. **运行完整 HS 实验**（更多迭代），用 `plot_trajectory.py` 生成论文轨迹图
2. **修复 2 个遗留测试失败**：`test_bcd_metadata_recorded` 键名对齐 + `test_condition_t_equals_0` mock LLM
3. **验证 D1 BCD 路径**：在 run_all_experiments 启用 `use_bcd_loop=True` 运行 D1 基准对比
