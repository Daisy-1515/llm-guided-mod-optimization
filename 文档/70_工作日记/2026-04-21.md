# 2026-04-21 工作日记

## 晚间（/endday）

### ✅ 完成的工作

- 为 `scripts/run_all_experiments.py` 补齐 `--tau` / `--tau-min` / `--tau-max` 参数覆盖能力，并把 `tau_min` / `tau_max` 写入实验输出元数据
- 把 A 组 tau sweep 输出目录固定为 `discussion/tau_sweep_a_seed42/numTasks_XX/tau_X/...` 的 task-count-first 布局
- 断电 / 退出后多次核对 `discussion/tau_sweep_a_seed42` 的实际落盘状态，识别 complete / partial / missing 点位
- 恢复并观察 `tau=1.5` 的补跑，最终确认已完成：
  - `numTasks=5` 的四档 tau
  - `numTasks=10/15/20/25/30/35` 的 `tau=1.5`
- 编写分机执行手册：`文档/30_测试与执行/A组_tau_sweep_seed42_实验操作手册_2026-04-21.md`
- 把最新实验状态和待补点位（共 30 个）写入操作手册，便于分发到其他机器执行

### 📝 修改文件

- `scripts/run_all_experiments.py`
- `.claude/rules/progress.md`
- `文档/30_测试与执行/A组_tau_sweep_seed42_实验操作手册_2026-04-21.md`
- `日记/2026-04-21.md`

### ⏳ 当前实验状态

- 当前 `discussion/tau_sweep_a_seed42` 下已无活跃 `run_all_experiments.py` 进程
- 半成品点位：`numTasks=10` 的 `tau=0.5/1.0/2.0`
- 完全缺失点位：`numTasks=15~50` 中除 `tau=1.5` 已完成部分外的剩余组合
- 总计仍需补齐 **30 个点位**（3 个半成品 + 27 个未开始）

### ⚠️ 已知卡点

- 本机性能不足，不适合继续单机串行补完全部 sweep
- 工作区仍有与本次 tau sweep 无直接关系的旧改动，提交时不能混入
- 本次未跑全量 `pytest`，当前状态只做了脚本参数和结果目录核对

### 📌 下次开始建议

1. 按操作手册把剩余 30 个点位分发到其他机器
2. 优先补 `numTasks=10~20` 与 `40~50` 区间
3. 回收结果时只拷贝对应时间戳目录回主仓库
4. 全部补齐后再统一做 sweep 作图与分析

## 夜间补充（带宽盘点与分机手册整理）

### ✅ 完成的工作

- 盘点仓库中已有的带宽相关实验、脚本、结果目录与文档记录
- 新增 `文档/50_运行分析/2026-04-21_分机实验操作手册.md`，整理多机并行实验的统一基线、必做实验、具体命令、结果目录规范与回传模板
- 更新 `文档/INDEX.md`，加入分机实验操作手册入口
- 复核 `scripts/run_group_a_bandwidth_sweep.py`，确认其当前只覆盖 `B_up`，不覆盖 `B_down`

### ⚠️ 当前结论

- 仓库里已有带宽相关参数调优和实验准备，但还缺可直接作为正式结论的完整 bandwidth sweep 结果集合
- 若用于论文正式结论，应优先补 `B_up = B_down` 的 paired bandwidth sweep，而不是只依赖上行带宽 sweep 脚本
- 当前工作区仍是混合状态，不适合直接自动提交


## 第四次更新（最终实验归档 + 无人机 CSV 回填）

### ✅ 完成的工作

- 把最终实验相关手册统一收拢到 `文档/30_测试与执行/最终实验手册/`
- 新增 `文档/30_测试与执行/最终实验现状总览_2026-04-21.md`，按真实结果目录核对完成度
- 将分散的无人机数量消融结果统一归档到 `最后实验/横坐标无人机 消融实验/`
- 补充目录说明 `最后实验/横坐标无人机 消融实验/README.md`
- 参考任务数表样式，填写 `最后实验/横坐标无人机 消融实验/横坐标无人机数量 消融实验.csv`
- 将任务数参考 CSV 保持在 `最后实验/横坐标任务数 消融实验.csv`

### 📝 修改文件

- `文档/30_测试与执行/最终实验手册/实验操作手册_分机执行.md`
- `文档/30_测试与执行/最终实验现状总览_2026-04-21.md`
- `最后实验/横坐标无人机 消融实验/README.md`
- `最后实验/横坐标无人机 消融实验/横坐标无人机数量 消融实验.csv`
- `最后实验/横坐标任务数 消融实验.csv`

### ⚠️ 已知卡点

- 按你要求尝试补跑 `D1` 无 BCD 的无人机数量 sweep（`numTasks=20`，`B_up=B_down=1e7`，`--no-bcd-loop`）时，当前机器被 Gurobi size-limited license 直接拦住
- 失败运行只生成了 `manifest.json`，本次会话已全部清理，未保留无效目录

### 📌 下次开始建议

1. 在具备完整 Gurobi 授权的机器上补跑 `D1` 的 `u=3~9`
2. 跑完后再把无人机数量 CSV 补成正式对比表
3. 基于任务数 / 无人机数量两张 CSV 统一出图

## 第五次更新（D1 无人机消融补跑）

### ✅ 完成的工作

- 先把本地 `master` 与远程 `origin/master` 同步到 `f1fc86f`
- 并行执行 7 组 D1 无人机数量消融实验，统一参数为：`seed=42`、`num_tasks=20`、`tau=1.0`、`b_up=b_down=1e7`、`hs_pop_size=3`、`hs_iterations=3`、`--no-bcd-loop`
- 新结果统一落盘到 `最后实验/横坐标无人机 消融实验/u3~u9/20260421_203309/`
- 核对 `manifest.json`，确认该批结果均为 `groups=["D1"]` 且 `use_bcd_loop=false`
- 更新 `最后实验/横坐标无人机 消融实验/横坐标无人机数量 消融实验.csv`，补入 `D1` 与 `D1 feasible` 两列，并按可行率优先、成本次之重算 `最佳可行方案`

### 📊 本批结果

- `u=3`：`best_cost_mean ≈ 286.8845`，`feasible_rate_mean = 1.0`
- `u=4`：`best_cost_mean ≈ 267.3361`，`feasible_rate_mean = 1.0`
- `u=5`：`best_cost_mean ≈ 265.8481`，`feasible_rate_mean = 1.0`
- `u=6`：`best_cost_mean ≈ 260.3082`，`feasible_rate_mean = 1.0`
- `u=7`：`best_cost_mean ≈ 260.4286`，`feasible_rate_mean = 1.0`
- `u=8`：`best_cost_mean ≈ 268.4240`，`feasible_rate_mean = 1.0`
- `u=9`：`best_cost_mean ≈ 263.5207`，`feasible_rate_mean = 1.0`

### 📝 结果口径更新

- 当前无人机消融汇总 CSV 中：`u=3/7` 的最佳可行方案仍为 `A`
- `u=4/5/6/8/9` 的最佳可行方案已切换为 `D1`

### ⚠️ 备注

- 本次没有运行 `uv run pytest tests -v`
- 本次补跑的是 D1，不解决 A 组 `u=5` 缺口
- 工作区当前包含新生成的 D1 结果目录和 1 个 CSV 修改，尚未提交

## 第六次更新（同步 D1 无人机消融结果）

### ✅ 完成的工作

- 将远端新增的 `D1` 无人机数量消融结果同步回本地
- 确认 `u=3~9` 的 `D1` 结果已落在 `最后实验/横坐标无人机 消融实验/uX/20260421_203309/`
- 确认无人机数量汇总表已扩展为 `A + D1` 对比口径
- 确认本地 `master` 与 `origin/master` 已重新对齐

### 📝 当前状态

- 无人机数量消融目录当前同时包含 `A` 与 `D1` 两组结果
- 对比表：`最后实验/横坐标无人机 消融实验/横坐标无人机数量 消融实验.csv`
- 当前工作区在同步后已恢复干净，仅补这一条日记说明

### 📌 备注

- 本次是结果同步与核对，不是本机重新求解
- 后续若继续做论文图，直接基于现有 CSV 出图即可
