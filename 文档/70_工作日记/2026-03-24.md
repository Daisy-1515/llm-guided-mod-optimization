# 2026-03-24 工作日记

## 会话 1：任务生成逻辑理解与文档修正

### 完成的工作

#### 双向任务生成逻辑的深度理解
- 澄清了任务生成的两层含义：
  - **第 1 层**：起飞前离线生成所有任务参数 `(D_l, D_r, F, τ)`
  - **第 2 层**：活跃时隙 `ζ_i^t` 动态决定何时产生任务（跨时隙处理）
- 识别了关键时间约束：任务必须在活跃时隙开始处理
- 通过代码探索验证了预计算与时延约束的对应关系

#### 文档与代码一致性修正
- **问题**：文档 L1-C1 约束缺失活跃条件 `ζ_i^t`
- **原约束**：`x_{i,i}^t + Σ x_{i,j}^t = 1`
- **修正后**：`x_{i,i}^t + Σ x_{i,j}^t = ζ_i^t`
- **补充说明**：当 `ζ_i^t = 0` 时无任务，隐含决策变量为 0
- **提交**：commit `fbf3372`

#### 项目理解深化
- 明确了时延约束的跨时隙隐式处理机制
- 验证了代码实现与论文模型的完整对应关系
- 确认了预计算过滤作为时延约束的执行机制

### 修改文件
- 文档/10_模型与公式/公式与两层解耦整合版_最新版_2026-03-24.md
- MEMORY.md
- 文档/70_工作日记/2026-03-24.md

---

## 会话 2：Phase⑥ Step 3 trajectory_opt.py 实现

### 完成的工作

#### 核心代码实现：trajectory_opt.py
- `solve_trajectory_sca()`：SCA 外层迭代框架
  - 预输入验证与计算预检
  - 最多 5 轮 SCA 迭代
  - Solver 降级策略：`CLARABEL -> ECOS -> SCS`
  - 完整诊断输出：历史、目标值、slack、耗时
- `TrajectoryOptParams`：轨迹优化参数数据类
  - 推进参数：`η1, η2, η3, η4, v_tip`
  - 轨迹参数：`v_max, d_safe`
- `TrajectoryResult`：轨迹优化结果数据类
  - 最优轨迹 `q[j][t] = (x, y)`
  - 真实推进能耗
  - 分 UAV 能耗分解
  - 完整诊断数据

#### 约束与辅助模块
- 6 类约束完整实现，对应论文公式 `(4a) - (4f)`
  - 地图边界
  - 起点固定
  - 终点固定
  - 速度约束（SOC）
  - 通信延迟约束
  - 安全距离约束（SCA 线性化 + slack）
- 辅助模块齐备：
  - `_validate_input_basic()`
  - `_validate_initial_trajectory()`
  - `_extract_active_offloads()`
  - `_build_sca_subproblem()`
  - `_rate_lower_bound_expr()`
  - `_propulsion_upper_bound_expr()`
  - `_propulsion_power_drag_ub()`
  - `_evaluate_true_objective()`

#### 配置扩展：config.py
- 新增 `[edgeUavBCD]` 节
- 新增参数：
  - `v_traj_max = 15.0`
  - `d_safe_traj = 5.0`
  - `max_sca_iter = 5`
  - `eps_sca = 1e-3`
  - `safe_slack_penalty = 1e3`

#### 单元测试框架：test_trajectory_opt.py
- 核心测试设计 `T1-T6` 已搭建
  - 端点固定情景
  - 边界/速度验证
  - 不可行端点检测
  - 通信约束驱动
  - 安全距离检查
  - SCA 收敛元数据
- 补充测试设计 `T7-T12` 已列出
  - Empty offload slot
  - 初值不安全 + slack 容错
  - 负通信预算快速失败
  - Solver 降级策略验证
  - 默认场景集成测试
  - 结果字段完整性检查

#### 设计决策确认
- `f_edge` 维度统一为 `[j][i][t]`
- `propulsion` 对接参数采用 `include_terminal_hover=False`
- 安全距离只在中间时隙 `0 < t < T-1` 检查，允许端点重叠
- 参数管理采用 `TrajectoryOptParams` 独立数据类
- 诊断输出保留完整 SCA 迭代历史
- Solver 采用逐级降级尝试

#### Git 提交
- commit `1c7cf1e`

### 修改文件
- edge_uav/model/trajectory_opt.py (+727)
- config/config.py (+14)
- tests/test_trajectory_opt.py (+488)

### 当前阶段状态
- `Phase⑥ Step 1 (propulsion.py)`：完成并验证
- `Phase⑥ Step 2 (resource_alloc.py)`：完成并验证
- `Phase⑥ Step 3 (trajectory_opt.py)`：代码完成，求解器集成待调试

### 已知卡点
- `tau` 参数偏紧，约 90% 任务不可行
- 问题二处理策略仍是决策阻断点
- CVXPY 求解器集成异常：`CLARABEL / ECOS / SCS` 首轮子问题返回 `None`

### 后续行动
1. git push（当日累计提交尚未推送）
2. 决策问题二的处理方式
3. Phase⑥ Step 4：`bcd_loop.py`
4. CVXPY 问题诊断与修复
5. 论文第二章基本原理

---

## 当日汇总

- 通过文档交叉核对，补齐了任务生成模型中的活跃时隙约束
- 完成了 `Phase⑥ Step 3 trajectory_opt.py` 的主体实现、配置扩展和测试框架搭建
- 当日结束时，`Phase⑥ Step 1-3` 已全部落地，主要剩余风险是求解器集成与问题二策略决策
