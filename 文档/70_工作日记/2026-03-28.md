## 2026-03-28 工作日记

### ✅ 完成的工作

- 新增 `scripts/run_all_experiments.py`，用于批量运行 Edge-UAV 实验组并统一输出结果。
- 调整 `config/config.py` 默认参数，将 `f_max` 从 `5e9` 提高到 `1e10`。
- 修改 `testEdgeUav.py`，移除运行前强行放宽 `tau` 和 `f_local` 的默认兜底逻辑。
- 撤销了本次会话中失败的“全仓注释中文化”尝试，恢复工作区到可提交状态。
- 完成根目录 Python 清理：
  - 将入口脚本迁移到 `scripts/`
  - 将原始 MoD 共享模块迁移到 `legacy_mod/`
  - 提交：`9c78066` `refactor(structure): move entry scripts out of repo root`
- 将 `PHASE4_DIAGNOSTIC_GUIDE.md` 迁移到 `文档/40_审查与诊断/Phase4_诊断指南_2026-03-27.md`
  - 提交：`5cd9499` `docs(diag): move Phase4 diagnostic guide into audit docs`

### 📝 修改统计

- 已跟踪代码/文档重构提交：
  - `a714eba` `feat(experiments): add batch experiment runner and tighten defaults`
  - `9c78066` `refactor(structure): move entry scripts out of repo root`
  - `5cd9499` `docs(diag): move Phase4 diagnostic guide into audit docs`
- 结构层结果：
  - 根目录业务 Python 入口已清理
  - `scripts/` 成为统一运行入口
  - `legacy_mod/` 承载旧主线共享模块

### 🔎 验证

- 已确认 `scripts.*` 和 `legacy_mod.*` 的导入成功。
- 已确认 `scripts/analyze_results.py --help` 可正常运行。
- 已确认根目录 `.py` 基本清空，仅保留项目级 `__init__.py`。
- 对 `tests/test_analyze_results_experiments.py` 做过最小回归尝试。
  - 导入层无问题
  - pytest 因系统临时目录权限拒绝未能跑通，不是代码导入错误

### ⚠️ 当前未收尾事项

- 工作区当前还有未提交删除：
  - `LICENSE`
  - `dependencies.yml`
- 这两个删除已在今天明确记录，但尚未决定是否保留并提交。

### 📌 下次开始建议

1. 明确 `LICENSE` 和 `dependencies.yml` 的删除是否合理，避免误删基础文件。
2. 若继续根目录清理，优先处理历史 `.log`、`phase4_acceptance.sh` 等阶段性产物。
3. 后续所有运行、分析、诊断命令统一走 `scripts/` 目录，避免结构回退。

### 会话补记

- 梳理了项目结构与模块依赖关系，确认当前仓库是“原始 MoD 主线 + Edge-UAV 主线 + 共用 HS/LLM/config 基础层”的并存结构。
- 只读分析了一次完整运行为什么慢，确认主瓶颈在 Edge-UAV 路径的 `HS × BCD × SCA` 乘法放大，而不是单一函数。
- 给出了 profiling 优先级：先看单个个体耗时拆分，再看 BCD 四阶段耗时，最后细化到轨迹优化 SCA 与 solver fallback。
