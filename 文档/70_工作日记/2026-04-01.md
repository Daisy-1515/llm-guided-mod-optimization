# 2026-04-01 工作日记

## 完成的工作

- **Drop 机制完全移除**：从 Level-1 卸载模型中移除 penalty_drop / drop 失败机制
  - 超时任务（D > τ）不再被丢弃，通过 D/τ > 1 自然反映在目标值中
  - L1-C1 约束改为按 i 聚合（每个任务恰好分配一次）
  - setupVars 无条件创建所有活跃任务变量
  - 目标函数回退到纯 cost1 + cost2

## 修改文件

| 文件 | 行数 | 说明 |
|------|------|------|
| `edge_uav/model/offloading.py` | +52/-106 | 核心：移除 drop 变量/罚项，无条件 setupVars |
| `edge_uav/model/objectives.py` | +19/-55 | 规范源：移除所有 drop 引用 |
| `config/config.py` | +0/-3 | 移除 penalty_drop 默认值 |
| `edge_uav/model/bcd_loop.py` | +0/-2 | 移除 penalty_drop 传递 |
| `heuristics/hsIndividualEdgeUav.py` | +0/-2 | 移除 penalty_drop 传递 |
| `tests/test_l1_completeness.py` | +89/-232 | 重写为 5 个无 drop 测试 |
| `tests/test_bcd_offloading_fix.py` | +10/-9 | 修复 active 数据格式 |

**总计**: 7 文件, +160/-348

## 验证结果

- 133 passed, 2 skipped, 2 pre-existing failures
- Codex 审查: PASS WITH NOTES
  - Note 1: 工作区有计划外文件变更（PDF/文档），提交时已排除
  - Note 2: `validate_offloading_outputs()` 校验强度弱于注释描述（非本次引入）

## 提交

- `2c53c0e` refactor(L1): remove drop mechanism — pure cost1+cost2 objective

## 已知遗留问题

- `test_bcd_metadata_recorded`: 键名不匹配（`iterations` vs `bcd_iterations`）
- `test_condition_t_equals_0_is_integer_comparison`: 需要 LLM API 配置

## 下次开始建议

1. 修复 2 个 pre-existing 测试失败
2. 运行完整规模 HS 实验，验证 LLM 引导优化效果
3. 分析诊断指标趋势，评估目标函数多样性
