# 2026-04-28 工作日记

## 晚间（endday 收口）

### ✅ 完成的工作

- 统一最终实验 5 个 MATLAB 作图脚本中的算法命名与图例口径，覆盖：
  - `最后实验/横坐标任务 消融/plot_task_ablation_cost.m`
  - `最后实验/横坐标无人机 消融实验/plot_uav_ablation_cost.m`
  - `最后实验/横坐标任务 时延不同/plot_task_delay_comparison_cost.m`
  - `最后实验/横坐标任务 带宽不同/plot_task_bandwidth_cost.m`
  - `最后实验/失败率/shibai.m`
- 两张消融图统一为 `ALA / Default Objective / LOGO / LLM+HS`
- 时延参数图统一为 `LLM+HS (τ=0.5/1.0/1.5/2.0)`
- 带宽参数图统一为 `LLM+HS (1 Mbps ... 5 Mbps)`
- 失败率柱状图横轴改为 `LLM+HS` 与 `Default Objective`，柱子类别统一为 `本地处理 / UAV卸载 / 失败`
- 用 MATLAB R2025b 实际跑通上述 5 个脚本并逐图验收，确认图例和标签已按新口径落盘
- 提交图例脚本改动：`40f55b5 chore: update experiment plot labels`

### 📝 修改文件

- `最后实验/失败率/shibai.m`
- `最后实验/横坐标任务 带宽不同/plot_task_bandwidth_cost.m`
- `最后实验/横坐标任务 时延不同/plot_task_delay_comparison_cost.m`
- `最后实验/横坐标任务 消融/plot_task_ablation_cost.m`
- `最后实验/横坐标无人机 消融实验/plot_uav_ablation_cost.m`
- `MEMORY.md`
- `.claude/rules/progress.md`
- `文档/70_工作日记/2026-04-28.md`

### 🧾 今日 Git 提交

| 时间 | Commit | 内容 |
|------|--------|------|
| 19:48 | `40f55b5` | `chore: update experiment plot labels` |

### 📊 当日结论

- 当前最终实验图的算法命名已经统一到 `ALA / Default Objective / LOGO / LLM+HS` 这一套口径
- 这次改动不涉及求解逻辑，只影响最终出图脚本和图面展示名称
- MATLAB 实跑已经证明 5 张相关图能够正常出图，长图例也没有被裁切

### ⚠️ 当前提醒

- `最后实验/失败率/shibai.asv` 仍是本地 MATLAB 自动保存文件，当前保持未跟踪，未纳入版本控制
- 本次会话未运行 `uv run pytest tests -v`
- 如果论文、Word 或答辩材料中已经嵌入旧图，仍需要重新导出并替换

### 📌 下次开始建议

1. 重新导出这 5 张最终实验图到论文或汇报使用路径，核对图题、图注和正文口径是否一致。
2. 把正文中的旧命名（如“本地执行”“标准目标函数”“LLM”）统一替换为 `ALA / Default Objective / LOGO / LLM+HS`。
3. 若继续修改脚本、CSV 或实验汇总逻辑，先补一次 `uv run pytest tests -v`。
4. 评估是否需要把 `shibai.asv` 纳入本地忽略策略，避免下次 endday 再次混入。
