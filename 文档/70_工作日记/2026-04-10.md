# 2026-04-10 工作日记

## 主要工作

### 1. 文档：LLM 效果不佳根因分析 + 原论文对比（`385b3ab`）

新增 3 篇分析文档：
- **LLM 效果不佳根因分析**：5 层根因（HS 多样性崩溃→LLM 表达力受限→BCD 鞍点→评估函数灵敏度→参数配置偏移）
- **原论文与当前实现对比**：参数、约束、目标函数差异汇总
- **近期变更与系统状态**：当前系统全貌快照

同时回退昨日 BCD 调参：`bcd_max_iter` 10→5，`bcd_eps` 0.0001→0.001。

### 2. LLM 接入 L2b 轨迹优化层（`bf32eaa`）

将 LLM 从 L1（任务分配目标）扩展到 L2b（轨迹优化目标）：
- **新建** `edge_uav/prompt/traj_prompt.py`：`TrajectoryPrompts` 类，way1/2/3 三种 prompt 策略
- **trajectory_opt.py**：`_build_sca_subproblem` + `solve_trajectory_sca` 支持 `dynamic_traj_obj_func`（exec 执行 LLM 代码 + fallback 默认）
- **bcd_loop.py**：`run_bcd_loop` 透传 `dynamic_traj_obj_func` 到 L2b
- **hsIndividualEdgeUav.py**：`llm_layer="L2b"` 模式，新增 `_get_l2b_llm_response()` 方法；L2b 模式下 L1 使用默认目标
- **hsUtils.py**：新增 `extract_traj_code_hsIndiv()` 代码提取
- **config**：新增 `llm_layer` 参数（L1/L2b 切换），默认 L1
- 新增 3 个单测（T19-T21：valid/invalid/None 三种场景），164 passed 2 skipped

### 3. exec namespace 变量补充（`17206a4`）

LLM 生成的 `traj_obj_code` 引用了 `n_uavs` / `n_tasks_active`，但 `_build_sca_subproblem` 的 exec namespace 未提供这些变量，导致所有 LLM 代码回退默认目标（0/6 custom_obj）。修复后 LLM 代码可正常执行。

### 4. BCD 多起点重启机制（`d1f67dc`）

逃离 BCD 鞍点的核心改进：
- **precompute.py**：新增 `_init_trajectory_random_visit()` — 随机打乱任务访问顺序生成多样初始轨迹；`InitPolicy` 加入 `"random_visit"`
- **bcd_loop.py**：`run_bcd_loop()` 新增 `bcd_num_restarts` 参数 — 收敛后递归重启 N 次取全局最优；`solution_details` 记录 `bcd_restarts_tried`
- **config**：`bcd_num_restarts = 1`（setting.cfg + config.py）
- D1 组同步透传 `bcd_num_restarts`（公平对比）
- 新增 2 个重启相关单测，166 passed 2 skipped

### 5. HS 种群多样性崩溃修复（`313bf27`）

针对根因分析识别的"Gen2 起所有个体得分完全一致"问题：
- **hsSorting.py**：新增 `hsDedupSorting` — 同分个体限 `max_same_score=2`，去重后按分数排序
- **hsFrame.py**：启用 `hsDedupSorting` 替代 `hsSorting`
- **hsPopulation.py**：`force_elite` 概率 100%→30%；选择窗口 top 50%→top 80%
- **setting.cfg**：`popSize` 10→20，`HMCR` 0.9→0.7，`iteration` 5→8
- 全量回归：166 passed, 2 skipped

### 6. 参数调整（未提交）

`alpha` 350→1000（`setting.cfg`），尝试提升通信时延项在目标函数中的权重占比。

---

## 今日实验记录

共 12 次实验（`20260410_173120` ~ `20260410_205743`），按代码阶段分组：

### 阶段 A：L2b LLM 初版（17:31~17:50，3 次）

配置：`llm_layer=L2b`，BCD=off（LLM 代码直接作为目标）

| 实验 | 个体数 | 分数 | solver_cost | LLM 使用 |
|------|--------|------|-------------|----------|
| 173120 | 2 | 81.11 / 81.11 | — | 0/1（默认）|
| 174052 | 2 | 80.96 / 80.96 | 2.5244 | 0/1（默认）|
| 174654 | 2 | 81.11 / 81.11 | 70620.08 | **1/1（LLM）** |

LLM L2b 代码执行成功（score=81.11）但 solver_cost=70620（数量级失调，远大于默认的 ~2.5），说明 LLM 生成的权重系数过大。

### 阶段 B：namespace 修复后 + BCD=on（18:10~19:22，2 次）

| 实验 | 个体数 | 分数 | solver_cost | BCD 轮次 |
|------|--------|------|-------------|---------|
| 181011 | 3 | 75.99 / 75.99 / 75.99 | ~4.30 | 3 |
| 191103 | 3 | 75.99 / 76.95 / 79.35 | ~4.30 | 3 |

BCD 从 2 轮扩展到 3 轮。191103 出现 3 个体分化（75.99 / 76.95 / 79.35）。

### 阶段 C：多起点重启后（19:50~20:24，3 次）

| 实验 | 个体数 | Gen0 分数 | Gen1 分数 | BCD | 重启 |
|------|--------|-----------|-----------|-----|------|
| 195015 | 2 | 81.11 / 81.11 | 81.11 / 81.11 | 无 | 0 |
| 195821 | 2 | 80.95 / 80.96 | 80.95 / 80.95 | 2 | 1 |
| **200937** | **3** | **74.73 / 76.12 / 76.44** | **74.73 / 76.12 / 76.32** | **3-4** | **1** |

**200937 是今日最有信息量的实验**：3 个体真实分化（74.73~76.44），BCD 3-4 轮迭代，重启机制生效。

### 阶段 D：种群修复后（20:34~20:57，3 次）

| 实验 | 个体数 | 分数 | 备注 |
|------|--------|------|------|
| 203431 | 2 | 81.11 / 81.11 | LLM L2b 执行成功，BCD 无 |
| 204433 | 2 | 80.95 / 80.96 | BCD=2 轮，restart=1，LLM 回退默认 |
| 205743 | — | (空) | 实验未完成 |

种群修复（popSize=20, dedup）尚未在完整实验中验证（这几次可能用的还是旧 popSize）。

---

## 关键发现

1. **L2b LLM 通路已打通**：代码可执行，但生成的权重系数过大（solver_cost 70620 vs 默认 2.5），需要在 prompt 中更明确地约束系数范围
2. **LLM 代码仍大量回退默认**：多数实验显示 `response_format: "None Obj"`，prompt 格式与 LLM 输出匹配率低
3. **BCD 重启有效**：从 2 轮扩展到 3-4 轮，random_visit 初始化带来个体分化
4. **HS 多样性修复待验证**：dedup + 精英概率化 + popSize=20 已提交，需完整实验确认效果

## 遗留问题

- LLM L2b prompt 需优化：(1) 输出格式匹配率低 (2) 系数量级失调
- `alpha=1000`（未提交）是否导致 SCA 数值问题待验证
- 种群修复后的 popSize=20、iteration=8 完整实验尚未执行
- Gen0→Gen1 个体完全相同（HS 进化停滞）在小 popSize 实验中仍存在
