# 项目文件清理分析

> 分析日期：2026-03-20
> 基于 Edge UAV 管线的 import 依赖追踪

---

## 一、Edge UAV 活跃文件（必须保留）

| 文件/目录 | 用途 |
|-----------|------|
| `edge_uav/` | Edge UAV 核心包（data / model / prompt / scenario_generator） |
| `testEdgeUav.py` | Edge UAV 运行入口 |
| `heuristics/hsFrame.py` | HS 顶层框架（共用） |
| `heuristics/hsPopulation.py` | 种群管理（共用，含 edge_uav 分支） |
| `heuristics/hsIndividualEdgeUav.py` | Edge UAV 个体（LLM → Gurobi → evaluate） |
| `heuristics/hsSorting.py` | 种群排序（共用） |
| `heuristics/hsUtils.py` | `extract_code_hsIndiv()`（共用） |
| `config/config.py` | configPara（10 节 42 参数） |
| `config/setting.cfg` | 运行时配置（platform / model） |
| `llmAPI/llmInterface.py` | LLM 调用网关（懒加载） |
| `llmAPI/llmInterface_huggingface.py` | HTTP 后端实现 |
| `tests/` | 62 个单元/集成测试 |
| `analyze_results.py` | 结果分析 + S1-S4 判据检测 |
| `check_llm_api.py` | API 连通性检查 |
| `config/__init__.py` | 包初始化 |
| `llmAPI/__init__.py` | 包初始化 |
| `__init__.py` | 根包初始化 |

## 二、仅原 Taxi-Passenger 管线使用（Edge UAV 无依赖）

以下文件仅被 `testAll.py` → Taxi 管线引用，Edge UAV 入口 `testEdgeUav.py` 及 `edge_uav/` 包均未导入。

| 文件/目录 | 大小 | 说明 |
|-----------|------|------|
| `testAll.py` | 小 | Taxi 管线入口 |
| `scenarioGenerator.py` | 小 | Taxi 场景生成（读 inputs/ + instances/） |
| `dataCommon.py` | 小 | Taxi / Passenger / Task 数据类 |
| `model/milpModel.py` | — | Taxi MILP 基类 `generalModel` |
| `model/two_level/AssignmentModel.py` | — | Taxi 乘客-出租车分配模型 |
| `model/two_level/SequencingModel.py` | — | Taxi TSP 排序模型 |
| `simulator/SimClass.py` | 20K | Taxi 仿真环境 `SimEnvironment` |
| `prompt/basicPrompt.py` | — | Taxi 基础提示模板 |
| `prompt/modPrompt.py` | 96K | Taxi 修改提示（way1-4） |
| `heuristics/hsIndividual.py` | — | Taxi 基础个体类 |
| `heuristics/hsIndividualMultiCall.py` | — | Taxi 完整个体（Level-1 + Level-2 + 仿真） |
| `inputs/` | 16K | 出行时间矩阵 CSV（Chicago / downtown） |
| `instances/` | 16K | 乘客/出租车历史数据 CSV |
| `resExample/` | **4.1M** | Taxi 3×10 运行结果 JSON（30 个文件） |

## 三、完全孤立文件（两个管线都未使用）

| 文件 | 说明 |
|------|------|
| `config/firstLevelExample.py` | 废弃的独立赋值模型示例，无任何 import 引用 |
| `model/two_level/AssignmentModel_googleOR.py` | OR-Tools 替代实现，从未被集成 |

## 四、非代码辅助目录

| 目录 | 大小 | 说明 |
|------|------|------|
| `discussion/` | 60K | 首跑产出（5 个 run_id 归档目录） |
| `logs/` | 24K | 运行日志 |
| `plans/` | 72K | 规划文档（论文方向分析、章节计划等） |
| `文档/` | 344K | 开发文档 / 审查报告 / 公式推导 |
| `日记/` | 88K | 开发日记 |
| `论文/` | **8.1M** | 参考论文 PDF |
| `image/` | **2.3M** | 文档插图（Taxi 场景相关） |

## 五、工具链配置（保留）

`.claude/`、`.codex/`、`.gemini/`、`.vscode/`、`.gitignore`、`dependencies.yml`、`.venv/`

---

## 清理建议

### P0 — 可直接删除（零风险）

```
config/firstLevelExample.py
model/two_level/AssignmentModel_googleOR.py
```

### P1 — 建议删除（释放空间，Edge UAV 不依赖）

```
resExample/                          # 4.1M，Taxi 示例数据
image/                               # 2.3M，Taxi 文档插图
```

### P2 — 建议归档后删除（原 Taxi 代码，留作参考可打 tag 后移除）

```
testAll.py
scenarioGenerator.py
dataCommon.py
model/milpModel.py
model/two_level/AssignmentModel.py
model/two_level/SequencingModel.py
simulator/SimClass.py
prompt/basicPrompt.py
prompt/modPrompt.py
heuristics/hsIndividual.py
heuristics/hsIndividualMultiCall.py
inputs/
instances/
```

> 推荐方式：在当前 commit 上打 `git tag taxi-archive`，然后再删除上述文件。

### P3 — 按需保留

```
论文/                                # 8.1M，参考 PDF，看是否仍需要
discussion/                          # 60K，首跑归档，分析完可清理
logs/                                # 24K，运行日志，分析完可清理
```

### 预计可释放空间

| 级别 | 约释放 |
|------|--------|
| P0 | < 10K |
| P0 + P1 | ~6.4M |
| P0 + P1 + P2 | ~6.6M |
| P0 + P1 + P2 + P3 | ~14.7M |
